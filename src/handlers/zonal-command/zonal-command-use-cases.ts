/**
 * Create nw zonal command
 */
export const createZonalCommand = async (commandRequest?: any): Promise<string> => {
  try {
  } catch (error) {
    throw error;
  }
  return "";
};

/**
 * Update existing zonal command = as
 */
export const updateZonalCommand = async (): Promise<string> => {
  return "";
};

/**
 * Change zonal command's status
 */
export const changeZonalCommandStatus = async (): Promise<string> => {
  return "";
};

/**
 * Get zonal commands
 */
export const getZonalCommands = async (): Promise<any[]> => {
  return [];
};

/**
 * Get Single zonal command
 */
export const getSingleZonalCommand = async (): Promise<any> => {
  return {};
};
