import { strict } from "assert";

/**
 * Creates an authentication profile with provided parameters
 */
export const createAuthenticationProfile = async (): Promise<string> => {
  return "";
};

/**
 * Updates an exitin authentication profule
 */
export const updateAuthenticationProfile = async (): Promise<string> => {
  return "";
};

/**
 * Perform login action
 */
export const performLogin = async (): Promise<string> => {
  return "";
};

/**
 * Perform reset password action request
 */
export const recoverPassword = async (): Promise<string> => {
  return "";
};

/**
 * Perform reset password
 */
export const resetPasssword = async (): Promise<string> => {
  return "";
};

/**
 * Change account type
 */
export const changeAccountType = async (): Promise<string> => {
  return "";
};

/**
 * Change account active status
 */
export const changeAccountActiveStatus = async (): Promise<string> => {
  return "";
};
