/**
 * Create nw Department
 */
export const createDepartment = async (): Promise<string> => {
  return "";
};

/**
 * Update existing department = as
 */
export const updateDepartment = async (): Promise<string> => {
  return "";
};

/**
 * Change department's status
 */
export const changeDepartmentStatus = async (): Promise<string> => {
  return "";
};

/**
 * Get departments
 */
export const getDepartments = async (): Promise<any[]> => {
  return [];
};

/**
 * Get Single department
 */
export const getSingleDepartment = async (): Promise<any> => {
  return {};
};
